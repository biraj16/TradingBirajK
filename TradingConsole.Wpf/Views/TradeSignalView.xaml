<UserControl x:Class="TradingConsole.Wpf.Views.TradeSignalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:TradingConsole.Wpf.ViewModels"
             xmlns:converters="clr-namespace:TradingConsole.Wpf.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:TradeSignalViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="800" d:DesignWidth="1200" Background="#FF1E1E1E" Foreground="White">

    <UserControl.Resources>
        <converters:ScoreToColorConverter x:Key="ScoreToColorConverter"/>
        <converters:ValueToSignConverter x:Key="ValueToSignConverter"/>

        <DataTemplate x:Key="FactorDataTemplate">
            <Border BorderBrush="#FF444444" BorderThickness="0,0,0,1" Padding="8,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding FactorName}" VerticalAlignment="Center" Foreground="LightGray"/>

                    <TextBlock Grid.Column="1" Text="{Binding StabilityText}" FontStyle="Italic" Foreground="Gray" VerticalAlignment="Center" Margin="10,0"/>

                    <TextBlock Grid.Column="2" Text="{Binding FactorValue}" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="White"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Sentiment}" Value="Bullish">
                                        <Setter Property="Foreground" Value="#FF4CAF50"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Sentiment}" Value="Bearish">
                                        <Setter Property="Foreground" Value="#FFF44336"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>
        </DataTemplate>

        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="ValueStyle">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15" DataContext="{Binding NiftyAnalysisResult}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF2D2D30" CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="{Binding Symbol}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                    <TextBlock Text="{Binding LTP, StringFormat='{}{0:N2}'}" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Margin="10,0,0,0" FontSize="16" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="White"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding PriceVsCloseSignal}" Value="Above Close">
                                        <Setter Property="Foreground" Value="#FF4CAF50"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding PriceVsCloseSignal}" Value="Below Close">
                                        <Setter Property="Foreground" Value="#FFF44336"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                        <Run Text="("/><Run Text="{Binding PriceChange, StringFormat='+0.00;-0.00;0.00'}"/><Run Text=" "/><Run Text="{Binding PriceChangePercent, StringFormat='({0:P2})'}"/><Run Text=")"/>
                    </TextBlock>
                </StackPanel>
                <Border Background="#FF3E3E42" CornerRadius="3" Padding="8,4" Margin="0,10,0,0" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding MarketThesis, StringFormat='Thesis: {0}'}" FontStyle="Italic" HorizontalAlignment="Center"/>
                </Border>

                <Grid Margin="0,20,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource HeaderStyle}" Text="CONVICTION SCORE"/>
                        <TextBlock Style="{StaticResource ValueStyle}" Text="{Binding ConvictionScore}"
                   Foreground="{Binding ConvictionScore, Converter={StaticResource ScoreToColorConverter}}"/>
                        <TextBlock Text="{Binding FinalTradeSignal}" TextAlignment="Center" FontStyle="Italic" Margin="0,5,0,0" Foreground="LightGray"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource HeaderStyle}" Text="INTRADAY REGIME"/>
                        <TextBlock Text="{Binding MarketRegime}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource ValueStyle}">
                                    <Setter Property="Foreground" Value="Orange"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding MarketRegime}" Value="Intraday Trend Up">
                                            <Setter Property="Foreground" Value="LimeGreen"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding MarketRegime}" Value="Intraday Trend Down">
                                            <Setter Property="Foreground" Value="Salmon"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="Dynamic market character" TextAlignment="Center" FontStyle="Italic" Margin="0,5,0,0" Foreground="LightGray"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource HeaderStyle}" Text="IV RANK (90-Day)"/>
                        <TextBlock Text="{Binding IvRank, StringFormat='{}{0:F2}%'}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource ValueStyle}">
                                    <Setter Property="Foreground" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IvRank, Converter={StaticResource ValueToSignConverter}, ConverterParameter=30}">
                                            <Setter Property="Foreground" Value="LimeGreen"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IvRank, Converter={StaticResource ValueToSignConverter}, ConverterParameter=70}">
                                            <Setter Property="Foreground" Value="Salmon"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="Low IVR is ideal for buying options" TextWrapping="Wrap" TextAlignment="Center" FontStyle="Italic" Margin="0,5,0,0" Foreground="LightGray"/>
                    </StackPanel>
                </Grid>

                <Border BorderBrush="#FF444444" BorderThickness="1" CornerRadius="3" Padding="8" Margin="0,15,0,0">
                    <Grid MinWidth="300">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock Text="dVAH" Foreground="LightGray" FontSize="11" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding DevelopingVah, StringFormat='{}{0:N2}'}" FontWeight="SemiBold" FontSize="14" Foreground="#FFF44336" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <Rectangle Grid.Column="1" Width="1" Fill="#FF444444" Margin="10,0"/>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <TextBlock Text="dPOC" Foreground="LightGray" FontSize="11" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding DevelopingPoc, StringFormat='{}{0:N2}'}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <Rectangle Grid.Column="3" Width="1" Fill="#FF444444" Margin="10,0"/>

                        <StackPanel Grid.Column="4" HorizontalAlignment="Center">
                            <TextBlock Text="dVAL" Foreground="LightGray" FontSize="11" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding DevelopingVal, StringFormat='{}{0:N2}'}" FontWeight="SemiBold" FontSize="14" Foreground="#FF4CAF50" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FF2D2D30" CornerRadius="5">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="BULLISH FACTORS" FontWeight="Bold" FontSize="16" Foreground="#FF4CAF50" Background="#FF3E3E42" Padding="10" TextAlignment="Center"/>
                        <ItemsControl ItemsSource="{Binding DataContext.BullishFactors, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      ItemTemplate="{StaticResource FactorDataTemplate}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="2" Background="#FF2D2D30" CornerRadius="5">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="BEARISH FACTORS" FontWeight="Bold" FontSize="16" Foreground="#FFF44336" Background="#FF3E3E42" Padding="10" TextAlignment="Center"/>
                        <ItemsControl ItemsSource="{Binding DataContext.BearishFactors, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      ItemTemplate="{StaticResource FactorDataTemplate}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>